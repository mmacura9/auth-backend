version: "2"
sql:
- schema: "./repository/migration"
  queries: "./repository/query"
  engine: "postgresql"
  gen:
    go: 
      package: "repository"
      out: "./repository/sqlc"
      emit_json_tags: true
      emit_interface: true
      emit_empty_slices: true